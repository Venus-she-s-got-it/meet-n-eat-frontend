import { Container } from 'react-bootstrap'

const ReviewForm = ({ restaurantId }) => {
return (
    <Container>
        <Form>
            
        </Form>
    </Container>
)
}

export default ReviewForm